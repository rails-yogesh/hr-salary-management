# Base path (or absolute URL) the frontend calls for the API.
# In development, Vite proxies /api to the Rails server (see vite.config.ts).
# In the docker-compose deployment, nginx proxies /api to the backend container.
VITE_API_BASE_URL=/api/v1
